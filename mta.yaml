_schema-version: "3.2"
ID: emp.Employee1
version: 0.0.1

build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm install
        - npm run build:workspaces

modules:
  - name: emp.Employee1
    type: staticfile
    path: dist
    parameters:
      disk-quota: 256M
      memory: 256M